import { HasPrivilegeDirective } from './has-privilege.directive';

describe('HasPrivilegeDirective', () => {
  it('should create an instance', () => {
    const directive = new HasPrivilegeDirective();
    expect(directive).toBeTruthy();
  });
});
